# 接棒｜第一批完成，等待新放行

## 現在

第一版與 batch-001 已完成。最後當班：Codex。本批研究工作已收束，無持續研究排程，無新實驗許可。網站只播放已保存結果。

## 已確認

- 六次實驗全部到達 2,400 步。集中食物的最終鄰近比例三組都較高；存活數配對差異 +16、−11、0，不能推論群聚有存活優勢。
- 10 個測試通過：確定性、存檔恢復、能量／血緣、滅絕、額度過期與底線、停止、單一程序鎖。
- 獨立程序從六份完整存檔接續 100 步，與從頭運行一致；這是程序層的恢復驗證，未宣稱另一個模型已閱讀並接棒。
- 公開檔案使用明確 9 檔清單，原始 records 與 runtime 不公開。

## 亨的意見

「流程保持簡單」「不同的阿光要如何交接」：已採一份接棒、一位当班、一批一放行。

「完整創建一次（新 repo），創完跑一次完，給我看網站」：本批施工與研究授權；不是後續批次的長期授權。

## 下一步

等亨看網站給意見；只有取得新放行才開 batch-002。候選題：固定食物條件，把個體群聚傾向歸零，比較存活與血緣，分辨食物吸引與相互作用。不要重跑或覆蓋 batch-001，也不要把群聚直接稱為合作。

新阿光先核對 GitHub main 最新版本與是否有其他當班者，再讀 records/batch-001/觀察.md。若只是接手理解，可執行 `npm run verify`；這不建立新研究批次、不修改原始結果。

## 停在哪裡

- 正本：私有 GitHub `xssnhub/little-thing-lab` 的 main；以 `git rev-parse HEAD` 核對當前版本。
- 終態：`records/batch-001/{uniform,patchy}-{17,83,211}.json`，含食物、PRNG 狀態、個體與血緣。
- 重播／校驗：同目錄 batch.json、summary.json、hashes.json。完整狀態與程式一起留在 Git。
- 恢復範例：讀取 JSON 後呼叫 `advance(state, ticks)`；user_stop/resource_stop 的狀態須在新放行下明確解除 stopReason。extinction/population_limit 保留為終止結果。
- 網站：`https://little-thing-xs-lab.loyal-clock-7713.chatgpt.site`。發布狀態以 Sites 回條為準；專案身分在 `.openai/hosting.json`。
- 硬界線：20% 額度保留、4 小時上限；25%／3 小時 45 分鐘開始收束。runtime 放行檔是短效資料，不可當作下一批授權。
